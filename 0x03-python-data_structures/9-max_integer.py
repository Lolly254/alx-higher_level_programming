#!/usr/bin/python3

def max_interger(my_list=[]):
    if my_list == 0:
        return
    y = my_list[0]
    for j in my_list:
        if i > x:
            x = i
        else:
            i < x
    return(x)
